<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="0dp">

        <include layout="@layout/detail_layout_generic_module"/>

        <TextView
            style="@style/detail_section"
            android:text="@string/display"/>

        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:paddingBottom="8dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingTop="8dp"
            android:useDefaultMargins="true">

            <TextView android:text="@string/text_to_display"/>

            <EditText
                android:id="@+id/edit_text"
                android:layout_gravity="fill_horizontal"
                />

            <ImageView
                android:id="@+id/capture"
                android:layout_gravity="fill_horizontal"
                android:layout_columnSpan="2"
                />
        </GridLayout>

        <TextView
            style="@style/detail_section"
            android:text="@string/sensors"/>

        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="3"
            android:paddingBottom="8dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingTop="8dp"
            android:useDefaultMargins="true">

            <TextView android:text="anButton1"/>

            <ProgressBar
                android:id="@+id/progressBar1"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_gravity="fill_horizontal"
                android:max="1000"/>

            <TextView
                android:id="@+id/ispressed1"
                tools:text="released"/>

            <TextView android:text="anButton2"/>

            <ProgressBar
                android:id="@+id/progressBar2"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_gravity="fill_horizontal"
                android:max="1000"/>

            <TextView
                android:id="@+id/ispressed2"
                tools:text="released"/>

            <TextView android:text="anButton3"/>

            <ProgressBar
                android:id="@+id/progressBar3"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_gravity="fill_horizontal"
                android:max="1000"/>

            <TextView
                android:id="@+id/ispressed3"
                tools:text="released"/>

            <TextView android:text="anButton4"/>

            <ProgressBar
                android:id="@+id/progressBar4"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_gravity="fill_horizontal"
                android:max="1000"/>

            <TextView
                android:id="@+id/ispressed4"
                tools:text="released"/>

            <TextView android:text="anButton5"/>

            <ProgressBar
                android:id="@+id/progressBar5"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_gravity="fill_horizontal"
                android:max="1000"/>

            <TextView
                android:id="@+id/ispressed5"
                tools:text="released"/>

        </GridLayout>

    </LinearLayout>
</ScrollView>