<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="0dp">

        <include layout="@layout/detail_layout_generic_module"/>

        <TextView
            style="@style/detail_section"
            android:text="@string/sensors"/>

        <GridLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:columnCount="3"

            android:gravity="center"
            android:paddingBottom="8dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingTop="8dp"
            android:useDefaultMargins="true"
            >

            <TextView android:text=""/>

            <TextView

                android:layout_gravity="right"
                android:text="@string/dc"
                />

            <TextView
                android:layout_gravity="right"
                android:text="@string/ac"
                />

            <TextView android:text="@string/current_voltage"/>

            <TextView
                android:id="@+id/current_value_dc"
                android:layout_gravity="right"
                tools:text="9.0 V"
                />

            <TextView
                android:id="@+id/current_value_ac"
                android:layout_gravity="right"
                tools:text="0.0 V"
                />


            <TextView android:text="@string/min_voltage"/>

            <TextView
                android:id="@+id/min_value_dc"
                android:layout_gravity="right"
                tools:text="9.0 V"
                />

            <TextView
                android:id="@+id/min_value_ac"
                android:layout_gravity="right"
                tools:text="0.0 V"
                />


            <TextView android:text="@string/max_voltage"/>

            <TextView
                android:id="@+id/max_value_dc"
                android:layout_gravity="right"
                tools:text="9.0 V"
                />

            <TextView
                android:id="@+id/max_value_ac"
                android:layout_gravity="right"
                tools:text="0.0 V"
                />


        </GridLayout>

    </LinearLayout>
</ScrollView>